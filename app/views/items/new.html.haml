%h1 New item

- form_for([@recipe, @item]) do |f|
  = f.error_messages

  = render :partial => "form", :locals => {:f => f, :item => @item}
  
  %p= f.submit "Create"

= link_to 'Back', recipe_path(@recipe)
