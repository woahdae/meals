%li{:id => html_id(item)}
  = link_to "#{item.name}", item
  &bull;
  - if item.is_a?(Recipe)
    = link_to "X", remove_lists_path(:recipe_id => item.id), :method => :post
  - elsif item.is_a?(Food)
    = link_to "X", remove_lists_path(:food_id => item.id), :method => :post
