= form_for(@food, :url => food_url(@food)) do |f|
  = f.error_messages

  = render :partial => "form", :locals => {:f => f}

  %p= f.submit "Update"

= link_to 'Show', food_url(@food)
|
= link_to 'Back', foods_path
