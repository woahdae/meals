- for food in @foods
  = link_to food.name, food
  &bull;
  = link_to_remote "Add to list", :url => add_lists_path(:item_uid_id => food.uid.id), :method => :post
  %br

%br

= link_to 'New food', new_food_path
