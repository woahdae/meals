= content_for(:title, h(@food.name))

= render :partial => "/layouts/nutrition_table", :locals => {:record => @food}

%div.clear

= link_to 'Edit', edit_food_path(@food)
|
= link_to 'Back', foods_path
